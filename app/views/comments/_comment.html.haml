.row.proto-comments
  %h4.col-md-12
    Comments
    %span.num
    #{@comments.count}
  .col-md-8
    - @comments.each do |comment|
      .media
        .media-left
          = link_to image_tag(comment.user.avatar, size: "64x64"), user_path(@prototype.user.id), method: :get
        .media-body
          %h4#top-aligned-media.media-heading
            = comment.user.nickname
            %a.anchorjs-link{href: "#top-aligned-media"}
              %span.anchorjs-icon
          %p
            = comment.text
          %p
